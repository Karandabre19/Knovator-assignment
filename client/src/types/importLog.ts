export interface importLog {
  name: string;
}

export interface importLog {
  _id: string;
  fileName: string;
  totalFetched: number;
  totalImported: number;
  newJobs: number;
  updatedJobs: number;
  failedJobs: number;
  timestamp: string;
  createdAt: string;
  updatedAt: string;
}
